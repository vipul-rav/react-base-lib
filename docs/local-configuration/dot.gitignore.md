# CYBG : Base Web App / Docs / Local Configuration / .gitignore

A basic gitignore is included. Items of note:

- The dist section ensures that the dist folder is included but the content is ignored.
